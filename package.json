{
  "name": "hackathon-shopify-starter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --names \"API,WEB\" --prefix-colors \"yellow,cyan\" \"cd backend && python3 -m uvicorn app.main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "setup": "node scripts/preflight.js && cd frontend && npm install && echo '✓ Frontend ready' && cd ../backend && pip3 install -r requirements.txt && echo '✓ Backend ready'",
    "sync": "cd backend && python3 -c \"import asyncio; from app.sync import run_full_sync; asyncio.run(run_full_sync())\"",
    "seed": "python3 backend/seed.py"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
